[phases.setup]
nixPkgs = ["nodejs", "python311", "mongodb"]

[phases.install]
cmds = [
  "cd frontend && npm install",
  "cd backend && pip install -r requirements.txt"
]

[start]
cmd = "uvicorn backend.server:app --host 0.0.0.0 --port $PORT"
